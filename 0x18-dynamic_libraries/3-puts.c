#include "main.h"

/**
 * _puts -  function that prints a string, followed by a new line, to stdout.
 *
 * @str: pointer to the first character on the string.
 *
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}





